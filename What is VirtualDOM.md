VirtualDOM

• React copies actual DOM
• React creates an actual lightweight representation of the actual Document under the hood.
• It can use different comparisons to figure out the minimal number of actual DOM operations it needs to make desired updates to the document. 